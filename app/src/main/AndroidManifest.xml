<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.ap.edu.schuldvordering">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UsersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddUserActivity"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddUserActivity" />
        </activity>
        <activity android:name=".FragmentsHolder"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddUserActivity" />
        </activity>

        <activity android:name=".AddVerplaatsingActivity"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddUserActivity" />
        </activity>
        <activity android:name=".AddVerblijfActivity"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddUserActivity" />
        </activity>
        <activity android:name=".AddHotelActivity"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddOverigeActivity" />
        </activity>
        <activity android:name=".AddOverigeActivity"
            android:parentActivityName=".UsersActivity">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AddUserActivity" />
        </activity>

    </application>

</manifest>